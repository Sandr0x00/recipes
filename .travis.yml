
# matrix:
  # include:
    # - language: python
      # python: 2.7
      # before_script:
      #   - cd backend/tests
      # script:
      #   - python -m unittest discover

    # - language: node_js
    #   node_js: "11"
    #   before_script:
    #     - npm install
    #   script:
    #     - npm run build

language: python
dist: xenial
node_js:
  - "11"
python:
  - "3.7"

cache:
  directories:
    - node_modules

install:
  - npm install
script:
  - npm run build
